# Encryption and Key Management

**System ID:** SEC-001

Encryption in transit (HTTPS/TLS) and at rest. Keys: environment-specific, rotatable, versioned, audited, never in source control.

Rotation: create → deploy overlap → migrate → verify → retire → audit
