# Provisioning Validation Standard

Blocking checks: owners, MFA, workspace ownership, policies, training/pilot workspaces, restricted capabilities disabled, isolation.

**Engine:** `src/lib/provisioning/validate.ts`
