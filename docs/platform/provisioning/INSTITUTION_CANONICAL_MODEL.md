# Institution Canonical Model

**PRV-001** — First-class `Institution` entity with owners, policies, launch stage, and deployment profile.

Records: `Institution`, `InstitutionProvisioningRequest`, `ProvisioningRiskAssessment`, `InstitutionOwnerAssignment`, `InstitutionWorkspace`, `InstitutionModule`, `ProvisioningValidation`.
