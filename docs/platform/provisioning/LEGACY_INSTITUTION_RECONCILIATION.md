# Legacy Institution Reconciliation

Reconcile existing institutions: inventory, assign owners, map template, validate, record deviations.

**Statuses:** `reconciled`, `reconciled_with_exceptions`, `needs_remediation`, `legacy_restricted`, `archived`
