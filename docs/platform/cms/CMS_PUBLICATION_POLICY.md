# CMS Publication Policy

Publication requires validation of approval status, audience, scope, channels, accessibility fields, and review dates. Every publish creates a `PublicationManifest` with rollback reference.

Scheduled publishing uses `scheduled_at` on content items. Expired content transitions to `expired` status.
