# CMS Localization Model

Translation-ready: each `ContentItem` may have linked translations tracking `source_version_id`. Drift states: `current`, `needs_review`, `outdated`, `missing`.

**Flag:** `CMS_TRANSLATIONS_ENABLED` — default false.
