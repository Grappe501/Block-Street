# CMS Test Plan

## Integration Flow

Author creates article → submit review → editor requests changes → revise → approve → schedule → publish → public API serves published only → search indexes → restricted content denied cross-org.

## Security

Draft not public · sanitize HTML · slug scope · AI cannot publish
