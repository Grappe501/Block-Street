# API File Upload Standard

**System ID:** API-001

Flow: request authorization → signed upload target → upload → validate → scan → create record. Signed URLs: short-lived, size-limited, content-type-limited, org-scoped.
