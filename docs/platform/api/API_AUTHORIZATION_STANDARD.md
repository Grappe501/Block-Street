# API Authorization Standard

**System ID:** API-001

Evaluate: Actor + auth strength + membership + role + permission + policy + resource ownership + data classification. Server-side enforcement on every endpoint. Resource-level authorization for counties, workspaces, content, missions.
