# Admin Configuration Governance

Configuration precedence: Platform Default → Organization Override → Workspace Override → User Preference.

Changes are versioned in `data/admin/system_configuration.json` with impact preview before activation.

Sensitive values stored as references, never plaintext in admin UI.
