# Admin Data Operations Policy

Bulk operations support dry-run preview. Risk levels: low (preview), moderate (approve import), high (export restricted data, destructive migration).

**API:** `POST /api/admin/data/operations/preview` · `/execute`
