# Admin Audit Standard

Administrative audit events are append-only, tamper-evident, searchable, privacy-safe. Never log passwords, tokens, or recovery codes.

**Store:** `data/admin/audit_events.jsonl` · **API:** `GET /api/admin/audit`
