# Admin Support Access Policy

Support sessions: explicit permission, reason, time limit, read-only default, visible banner, full audit trail. No credential access.

**API:** `POST /api/admin/support/session` (scaffold)
