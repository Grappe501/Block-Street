# CI Standard

**System ID:** DPL-001

GitHub Actions on pull request and push to `main`: install → lint → typecheck → build → validate:release

Required checks block merge on protected branches. Workflow changes require dedicated review.
