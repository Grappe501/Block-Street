# Template Evolution Standard

Templates version forward — history is never overwritten. Each evolution record tracks version, source Objective, lessons applied, and lifecycle state (draft → validated → recommended → institution standard → historical → retired).
