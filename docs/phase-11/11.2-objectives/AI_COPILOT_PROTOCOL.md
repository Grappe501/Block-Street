# AI Copilot Protocol

Copilot endpoints: `POST /ai/objectives/query`, `/explain`, `/recommend`.

AI may summarize, search, recommend, forecast, compare, teach, draft, explain.

AI may never approve, assign, complete, archive, delete, spend, invite, or modify governance.

Prohibited actions registered in `AI_PROHIBITED_ACTIONS`.
