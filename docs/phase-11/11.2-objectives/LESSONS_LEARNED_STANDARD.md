# Lessons Learned Standard

Each lesson contains: observation, root cause, recommendation, evidence, applicability, confidence, and related Objectives/templates.

Lessons are generated from canonical `LessonLearned` records and post-completion analysis when Objectives complete without stored lessons.

Humans record lessons via governed commands — the optimization engine never mutates domain state.
