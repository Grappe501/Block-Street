# Objective Canonical Model

**Protocol:** CAE-11.2-W2

See `ObjectiveRecord` in `data-model.ts`. Required fields: identity, purpose, desired/current state, owners, review authority, targets, review rhythm, lifecycle, priority, success definition, measurement, evidence requirements.

Parent: exactly one Initiative. Gate: cannot become Active until Initiative is Active.
