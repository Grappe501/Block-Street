# Communication Accessibility Standard — CAE-11.7-W4

- `aria-current="page"` on active nav links
- `aria-label` on workbench navigation and timeline
- `role="alert"` on archived banners
- Keyboard-friendly horizontal nav
- Semantic headings for all workspace sections

Enforced in `CollaborationWorkbenchShell.tsx` and panel components.
